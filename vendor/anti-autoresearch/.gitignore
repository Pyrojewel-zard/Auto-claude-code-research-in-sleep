# eval scratch (regenerated by run_eval.py)
eval/.eval_run/

# generated synthetic corruptions (regenerated from clean/ by run_eval.py)
eval/fixtures/synthetic_corruptions/*.tex

# run outputs from auditing real papers
*.findings.json
claims.json
report.json
REPORT.md
artifact_manifest.json
adversarial-case-builder.memo.md
.aris/

# python
__pycache__/
*.pyc

# os / editor
.DS_Store

# The deterministic eval fixture is required by eval/run_eval.py and is part
# of the vendored release, despite the generic runtime-output ignore above.
!eval/fixtures/evidence_branch/claims.json
